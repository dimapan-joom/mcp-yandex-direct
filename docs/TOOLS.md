# Инструменты

Сервер предоставляет типизированные инструменты для частых объектов плюс универсальный `raw_request` для любого другого сервиса/метода — так доступен **весь API v5**.

Каждый инструмент (кроме `get_balance` и `list_agency_clients`) принимает опциональный параметр `login` — логин клиента для заголовка `Client-Login`. С агентским токеном это позволяет одним сервером работать со всеми клиентскими аккаунтами: логины отдаёт `list_agency_clients`, без `login` запрос уходит в аккаунт из `YANDEX_DIRECT_LOGIN` или в собственный аккаунт владельца токена.

## Аккаунт и справочники

| Инструмент | Что делает |
| --- | --- |
| `get_account_info` | Данные аккаунта: логин, валюта, тип, страна. |
| `list_agency_clients` | Клиенты агентства (agencyclients/get): логины, валюта, архивность. Только для агентского токена; запрос идёт без Client-Login. |
| `get_balance` | Баланс единого счёта (Amount, AmountAvailableForTransfer, Currency) через Live v4 `AccountManagement` — единственный метод API, отдающий баланс. Деньги в валюте счёта; отрицательная сумма = задолженность. |
| `get_quota` | Остаток дневной квоты API (Units: потрачено / осталось / лимит). |
| `get_regions` | Поиск id регионов по названию (нужны для `create_ad_group`). |
| `get_dictionaries` | Справочники (валюты, часовые пояса, константы, …). |

## Кампании, группы, объявления, ключи

| Инструмент | Что делает |
| --- | --- |
| `list_campaigns` / `create_text_campaign` / `update_campaign` / `campaign_action` | Список, создание, обновление (имя, бюджет, **минус-слова**), приостановка/возобновление/архив/удаление. |
| `list_ad_groups` / `create_ad_group` / `update_ad_group` / `delete_ad_groups` | Список, создание, обновление групп (имя, регионы, **минус-слова**), удаление по id. |
| `list_ads` / `create_text_ad` / `update_text_ad` / `ad_action` | Список, создание, обновление текстовых объявлений; модерация/приостановка/возобновление/архив/удаление. |
| `list_keywords` / `add_keywords` / `set_keyword_bids` / `keyword_action` | Список, добавление, ручные ставки, приостановка/возобновление/удаление фраз. |

## Корректировки ставок

| Инструмент | Что делает |
| --- | --- |
| `get_bid_modifiers` | Чтение корректировок (моб., десктоп, демография, ретаргетинг, гео, видео, SMART TV). |
| `add_bid_modifier` | Добавить корректировку на кампанию или группу. |
| `set_bid_modifiers` | Изменить процент существующих корректировок. Включить/выключить корректировку через API нельзя (метод `toggle` устарел) — вместо выключения удалить её. |
| `delete_bid_modifiers` | Удалить корректировки по id. |

## Расширения объявлений

| Инструмент | Что делает |
| --- | --- |
| `get_sitelinks` / `create_sitelinks_set` / `delete_sitelinks` | Наборы быстрых ссылок. |
| `get_callouts` / `add_callouts` / `delete_callouts` | Уточнения. |
| `get_vcards` / `create_vcard` / `delete_vcards` | Виртуальные визитки. |
| `get_ad_images` / `get_ad_videos` / `get_creatives` | Чтение библиотек изображений / видео / креативов. |
| `upload_ad_image` | Загрузка изображения (по URL или base64) в библиотеку, возвращает `AdImageHash` для объявления. |

## Статистика и универсальный вызов

| Инструмент | Что делает |
| --- | --- |
| `get_statistics` | Отчёт через сервис Reports. Обычные типы (CAMPAIGN/ADGROUP/AD/CRITERIA/ACCOUNT) возвращают TSV-строки (сверх ~1 МБ / 100 тыс. строк ответ обрезается с явной пометкой `[_truncated]`); `SEARCH_QUERY_PERFORMANCE_REPORT` — вычисленную сводку (JSON: итоги по всем строкам + топ-N + хвост + счётчики нулевых кликов/конверсий). |
| `raw_request` | Прямой вызов любого сервиса/метода (полное покрытие API). Записи требуют `confirmWrite=true`. |

## Деньги и постраничная выдача

Денежные значения в типизированных инструментах — в валюте аккаунта в обе стороны: входные (бюджеты, ставки) автоматически переводятся в микроединицы, а вывод `list_*` пересчитывается обратно. `raw_request` работает с сырыми данными (микроединицы).

Списочные инструменты принимают `limit`/`offset` и флаг `autoPaginate`, который проходит все страницы по курсору `LimitedBy`. Объём `autoPaginate` ограничен (~1 МБ или 100 тыс. объектов): при остановке на лимите результат помечается `_truncated`/`_truncatedNote`, а `LimitedBy` содержит курсор для продолжения.

## Аннотации

Каждый инструмент помечен MCP-аннотациями (`readOnlyHint` / `destructiveHint` / `idempotentHint`): `list_*`/`get_*` — только чтение; `update_*`/`set_*` — идемпотентная запись; `create_*`/`add_*`/`upload_*` — создание объектов; `delete_*`, `*_action` и `raw_request` — потенциально необратимые. MCP-клиент может использовать их, чтобы автоматически подтверждать чтение и предупреждать перед записью.

## Переменные окружения

| Переменная | Обяз. | По умолчанию | Описание |
| --- | --- | --- | --- |
| `YANDEX_DIRECT_TOKEN` | один из режимов | — | Статический OAuth-токен (простой режим). Игнорируется, если задано OAuth-трио. |
| `YANDEX_OAUTH_CLIENT_ID` | с трио | — | ClientID приложения — режим самообновляющегося токена. |
| `YANDEX_OAUTH_CLIENT_SECRET` | с трио | — | Пароль приложения. |
| `YANDEX_OAUTH_REFRESH_TOKEN` | с трио | — | Refresh-токен; сервер сам обменивает его на access-токен и повторяет запрос при 401/коде 53. |
| `YANDEX_OAUTH_TOKEN_URL` | нет | `https://oauth.yandex.ru/token` | Переопределение OAuth-эндпоинта (только https). |
| `YANDEX_OAUTH_TIMEOUT_MS` | нет | `YANDEX_DIRECT_TIMEOUT_MS`, иначе `30000` | Таймаут обмена токена, мс. |
| `YANDEX_DIRECT_LOGIN` | нет | — | Заголовок `Client-Login` (агентские аккаунты). |
| `YANDEX_DIRECT_LANG` | нет | `ru` | Язык ответов API (`ru`, `en`, `uk`, `tr`). |
| `YANDEX_DIRECT_SANDBOX` | нет | `false` | Использовать песочницу API. |
| `YANDEX_DIRECT_TIMEOUT_MS` | нет | `60000` | Таймаут запроса, мс. |
| `YANDEX_DIRECT_MAX_RETRIES` | нет | `3` | Повторы при временных ошибках (лимиты запросов, 5xx). |
